# Use the official Python image from the Docker Hub
FROM python:3.12

# Set the working directory in the container
WORKDIR /app

# Copy the backend requirements file into the container
COPY ctetproject/backend/requirements.txt /app/

# Install the Python dependencies
RUN pip install -r requirements.txt

# Copy the entire backend project into the container
COPY ctetproject/backend/ /app/

# Copy the built frontend into the container
COPY ctetproject/frontend/build /app/frontend/build

# Set environment variables
ENV DJANGO_SETTINGS_MODULE=ctetproject.settings

# Expose the port the app runs on
EXPOSE 8000

# Run the application with Gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:8000", "ctetproject.wsgi:application"]